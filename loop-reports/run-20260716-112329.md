# succ-loop run 20260716-112329

- mode: supervised
- stop reason: **HARNESS_ERROR**
- base: 23a76a8  branch: loop/succ-directory-walk
- best: score [0, -6, 0, 0, -99] @ None
- checkpoints: []
- total cost this run: $7.25

## Attempts

| # | hypothesis | claim | verdict | gate class | g1 overlay | g1 div | g2 div | model | cost | dur s |
|---|-----------|-------|---------|-----------|-----------|--------|--------|-------|------|-------|
| 1 | H1-ledger-data-succ-walk | ready-for-gate | harness-error | harness | - | - | - | fable | $7.25 | 1006 |

## Hypotheses ruled out

- none

## Current lead

Key harness facts: (1) gate.sh unit only runs INTEGRATION test targets — in-module #[cfg(test)] tests in src/*.rs never execute; put offline tests in crates/xrpl-node/tests/*.rs against pub API. (2) cargo's test filter matches test FN names, not file/binary names — run with empty filter '' to be sure. (3) The unit run compiles the lib WITH ffi (a pre-existing ffi_engine doctest failed there), so ffi_engine edits are compile-checked by the unit gate; I fixed that pre-existing broken doctest (indented pseudo-code at old ffi_engine.rs:1326, from 479f6e1) by fencing as ```text — empty-filter runs were red at baseline without this. Design: pure walk/merge logic in src/succ_walk.rs (always compiled); RpcProvider::succ = walk_pages_for_succ over fetch_ledger_data_page (ledger_data, binary:true, limit:32, marker seeded with key, pinned ledger_index, 6-attempt rotate/backoff like read_with_outcome, max 8 pages); LayeredProvider::succ = layered_succ (tombstone-chain RPC probe + overlay min), overlay snapshot taken before walking so no lock held during HTTP. Risk if gate still shows too few offers crossing: (a) marker seeding semantics — if an endpoint rejects a marker that wasn't previously issued, walk returns None; check misses counter; (b) limit 32 could theoretically be rejected by some endpoint configs; (c) OverlayedDbProvider path unchanged (already had succ) — only the no-snapshot path gained succ.
